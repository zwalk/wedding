import { NavFixDirective } from './nav-fix.directive';

describe('NavFixDirective', () => {
  it('should create an instance', () => {
    const directive = new NavFixDirective();
    expect(directive).toBeTruthy();
  });
});
